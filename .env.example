# --- Blockchain data providers (server-only, never expose to the client) ---
BIRDEYE_API_KEY=
HELIUS_API_KEY=

# --- Supabase (current API key naming: publishable/secret, not anon/service_role) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=
DATABASE_URL=

# --- App ---
NEXT_PUBLIC_APP_URL=http://localhost:3000

# --- Helius webhooks (Phase 1G) ---
HELIUS_WEBHOOK_AUTH_HEADER=

# --- Internal job endpoints (optional locally; required once deployed — see supabase/CRON.md) ---
INTERNAL_JOB_SECRET=
